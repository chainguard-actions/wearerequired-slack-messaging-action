name: Test invalid token error
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Run action with invalid token and channel_id (expect failure)
        id: action
        uses: .
        continue-on-error: true
        with:
          bot_token: 'xoxb-invalid-token-000000000000-000000000000-XXXXXXXXXXXXXXXXXXXXXXXX'
          channel_id: 'C0000000000'
          payload: '{"text":"Hello from test"}'

      - name: Assert action failed due to invalid token
        run: |
          echo "outcome: ${{ steps.action.outcome }}"
          if [ "${{ steps.action.outcome }}" != "failure" ]; then
            echo "FAIL: expected action to fail with invalid Slack token"
            exit 1
          fi
          echo "PASS: action correctly failed with invalid Slack token"
