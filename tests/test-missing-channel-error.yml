name: Test missing channel error
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Run action without channel or channel_id (expect failure)
        id: action
        uses: .
        continue-on-error: true
        with:
          bot_token: 'xoxb-fake-token-for-testing'
          payload: '{"text":"Hello from test"}'

      - name: Assert action failed
        run: |
          echo "outcome: ${{ steps.action.outcome }}"
          if [ "${{ steps.action.outcome }}" != "failure" ]; then
            echo "FAIL: expected action to fail when no channel or channel_id provided"
            exit 1
          fi
          echo "PASS: action correctly failed when no channel or channel_id provided"
