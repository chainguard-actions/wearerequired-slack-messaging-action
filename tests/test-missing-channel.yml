name: Test missing channel input error handling
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Run action without channel or channel_id (expect failure)
        id: action
        uses: .
        continue-on-error: true
        with:
          bot_token: 'xoxb-invalid-token-for-testing'
          payload: |
            {
              "text": "Hello from GitHub Actions test"
            }

      - name: Assert action failed when channel is missing
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          if [ "$OUTCOME" != "failure" ]; then
            echo "FAIL: expected action to fail when no channel provided, but got outcome: $OUTCOME"
            exit 1
          fi
          echo "PASS: action correctly failed when channel is missing"
